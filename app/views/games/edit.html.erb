<h2> Edit Game of Thrones</h2>

<%= form_tag ("/games/#{@game['id']}", method: 'patch') do %>

<div>
  <%= label_tag(:character) %>
  <%= text_field_tag(:character, @game['character']) %>
</div>

<div>
  <%= label_tag(:house) %>
  <%= text_field_tag(:house, @game['house']) %>
</div>


<div>
  <%= label_tag(:city) %>
  <%= text_field_tag(:city, @game['city']) %>
</div>

<div>
  <%= submit_tag("submit") %>
</div>

<% end %>
